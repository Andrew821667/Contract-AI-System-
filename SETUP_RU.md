# 🚀 ИНСТРУКЦИЯ ПО ЗАПУСКУ - Contract AI System

## ✅ Проверка выполнена
- ✅ Python 3.11.14 установлен
- ✅ Файл .env создан
- ✅ Необходимые папки созданы
- ✅ Проект готов к запуску

---

## 📋 ВАРИАНТЫ ЗАПУСКА

### **Вариант 1: БЫСТРЫЙ DEMO (упрощенный сервер)**

Это самый простой способ проверить, что проект работает.

#### Шаг 1: Установите минимальные зависимости

```bash
pip3 install fastapi uvicorn python-dotenv python-multipart
```

#### Шаг 2: Запустите demo сервер

```bash
cd /home/user/Contract-AI-System-
python3 start_demo.py
```

#### Шаг 3: Откройте в браузере

- **API Docs**: http://localhost:8000/docs
- **Health Check**: http://localhost:8000/health
- **API Info**: http://localhost:8000/api/v1/info

---

### **Вариант 2: ПОЛНАЯ УСТАНОВКА (со всеми функциями)**

Для использования всех AI-функций нужно установить все зависимости.

#### Шаг 1: Установите все зависимости

```bash
cd /home/user/Contract-AI-System-
pip3 install --upgrade pip
pip3 install -r requirements.txt
```

⚠️ **Внимание**: Установка займет 5-10 минут и потребует ~2GB свободного места.

Если возникнут ошибки с компиляцией, установите системные пакеты:

```bash
# Ubuntu/Debian:
sudo apt-get update
sudo apt-get install build-essential libssl-dev libffi-dev python3-dev

# macOS:
brew install openssl
```

#### Шаг 2: Настройте API ключи

Откройте файл `.env` и добавьте ваши API ключи:

```bash
nano .env
```

Минимально необходимо добавить один из:
- `OPENAI_API_KEY=sk-...` (для GPT-4/GPT-3.5)
- `ANTHROPIC_API_KEY=sk-ant-...` (для Claude)

#### Шаг 3: Инициализируйте базу данных

```bash
python3 -c "from src.models.database import Base, engine; Base.metadata.create_all(bind=engine)"
```

#### Шаг 4: Запустите полный сервер

**Вариант A: FastAPI Backend**
```bash
python3 -m uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload
```

**Вариант B: Streamlit UI (веб-интерфейс)**
```bash
streamlit run app.py
```

#### Шаг 5: Откройте в браузере

**FastAPI:**
- **Swagger UI**: http://localhost:8000/docs
- **Health**: http://localhost:8000/health

**Streamlit:**
- **Web UI**: http://localhost:8501

---

## 🎯 ТЕСТИРОВАНИЕ ФУНКЦИЙ

### Проверка API через curl:

```bash
# Health check
curl http://localhost:8000/health

# API info
curl http://localhost:8000/api/v1/info
```

### Проверка через Python:

```python
import requests

# Test health endpoint
response = requests.get("http://localhost:8000/health")
print(response.json())
```

---

## 🔧 РЕШЕНИЕ ПРОБЛЕМ

### Проблема: "Port 8000 already in use"

```bash
# Найдите процесс
lsof -i :8000

# Остановите его
kill -9 <PID>
```

### Проблема: "Module not found"

```bash
# Убедитесь, что находитесь в корне проекта
cd /home/user/Contract-AI-System-

# Переустановите зависимости
pip3 install --upgrade -r requirements.txt
```

### Проблема: "Database connection error"

По умолчанию используется SQLite (не требует настройки).
Файл БД создается автоматически: `contract_ai.db`

---

## 📚 ДОКУМЕНТАЦИЯ

- **README**: `README.md` - Полное описание проекта
- **Quick Start**: `QUICK_START_RU.md` - Быстрый старт
- **API Docs**: http://localhost:8000/docs (после запуска)

---

## 🎉 ЧТО ДАЛЬШЕ?

1. **Изучите API** в Swagger UI: http://localhost:8000/docs
2. **Настройте API ключи** для LLM (OpenAI/Anthropic)
3. **Загрузите тестовый договор** через UI
4. **Попробуйте анализ договора** с AI

---

## 💡 ПОЛЕЗНЫЕ КОМАНДЫ

```bash
# Проверка версии Python
python3 --version

# Список установленных пакетов
pip3 list

# Обновление зависимостей
pip3 install --upgrade -r requirements.txt

# Очистка кэша Python
find . -type d -name __pycache__ -exec rm -r {} +

# Проверка логов
tail -f logs/api.log
```

---

## 📞 НУЖНА ПОМОЩЬ?

Если что-то не работает:
1. Проверьте версию Python: `python3 --version` (должна быть 3.11+)
2. Проверьте, что .env файл создан: `ls -la .env`
3. Проверьте логи: `tail -f logs/api.log`
4. Проверьте порт: `lsof -i :8000`

---

**Успешного запуска! 🚀**

Система готова к работе на вашем компьютере.
