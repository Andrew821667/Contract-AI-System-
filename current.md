# 📊 Contract AI System - Текущее состояние проекта

**Последнее обновление:** 2026-01-08
**Ветка разработки:** `claude/plan-idp-integration-2rpCO`
**Статус:** Планирование архитектуры 2.0

---

## 🎯 Цель проекта

Создать единую платформу для полного жизненного цикла договоров:
- **Pre-Execution:** Анализ черновиков, сравнение с шаблонами, риск-скоринг, протокол разногласий
- **Post-Execution:** Цифровизация подписанных документов в исполняемые данные

---

## 🏗️ Архитектура 2.0 - Ключевые решения

### Multi-Model Routing Strategy
- **DeepSeek-V3** → 90% задач, $0.14/1M токенов (основной worker)
- **Claude 4.5 Sonnet** → 10% задач (сложные сканы, плохое качество), $3.00/1M токенов
- **GPT-4o** → Резервный канал, $2.50/1M токенов
- **GPT-4o-mini** → Тестирование системы, $0.15/1M токенов (дешевая и умная для валидации)
- **Экономия:** Стоимость обработки документа с $0.50 → $0.02

### База данных
- **Гибридная схема:** PostgreSQL 16+ (SQL для ядра + JSONB для гибкости)
- **Ключевые таблицы:**
  - `contracts_core` - центральная таблица с JSONB attributes + raw_data
  - `contract_rules` - исполняемые правила (штрафы, SLA) в формате JSONB
  - `contract_parties`, `contract_items`, `payment_schedule` - нормализованные данные
- **Векторный поиск:** pgvector (embedding vector(1536))

### Pipeline обработки
1. **OCR/Layout:** PaddleOCR + LayoutLMv3
2. **Level 1:** Regex/SpaCy (даты, ИНН, суммы) - бесплатно
3. **Level 2:** LLM через Smart Router - таблицы, условия, риски
4. **RAG Filter:** На каждом этапе как фильтр для повышения точности

### Режимы работы системы
1. **Полная нагрузка:** Все модули пайплайна работают параллельно
2. **Последовательный (Эконом):** Модули включаются по очереди (этап прошел → модуль отключился → следующий включился). Для малых компаний с небольшим потоком документов.
3. **Ручной:** Пользователь выбирает какие модули включить

---

## 📋 План внедрения (20 недель, 5 этапов)

### Stage 0: Инфраструктура (Недели 1-2)
- [ ] Миграция БД: новые таблицы (contracts_core, contract_rules и др.)
- [ ] Настройка pgvector расширения
- [ ] Интеграция API: DeepSeek-V3 + Anthropic Claude
- [ ] Базовый Smart Router (заглушка)

### Stage 1: Post-Execution MVP (Недели 3-6)
- [ ] Загрузка подписанных документов
- [ ] Извлечение базовых фактов через DeepSeek-V3
- [ ] Сохранение в contracts_core (JSONB format)
- [ ] Валидация Pydantic-схемами
- [ ] API endpoints: `/contracts/digitize`, `/contracts/{id}/structured`

### Stage 2: Pre-Execution (Недели 7-10)
- [ ] Загрузка черновиков для анализа
- [ ] Сравнение с шаблонами/Playbook
- [ ] Risk Scoring Engine
- [ ] Генерация протокола разногласий
- [ ] API endpoints: `/negotiation/upload`, `/negotiation/{id}/disagreements`

### Stage 3: Smart Router Production (Недели 11-14)
- [ ] Оценка сложности документа (complexity_score)
- [ ] Автоматическое переключение DeepSeek ↔ Claude
- [ ] Пользовательский выбор модели (force_model parameter)
- [ ] Метрики качества и стоимости
- [ ] Fallback логика при недоступности модели

### Stage 4: Интеграции + UI (Недели 15-20)
- [ ] Обработка доп. соглашений (Amendment flow)
- [ ] Векторный поиск похожих договоров (pgvector)
- [ ] Интеграция с 1C/ERP (export/import)
- [ ] UI для юристов: редактор протокола разногласий
- [ ] Dashboard: мониторинг сроков, контроль обязательств

---

## 🔄 Workflow разработки

1. **Claude Code:** Пишет код, создает коммиты
2. **Push:** `git push -u origin claude/plan-idp-integration-2rpCO`
3. **Пользователь:** `git pull origin claude/plan-idp-integration-2rpCO`
4. **Тестирование:** Запуск на локальной машине, проверка функционала
5. **Обратная связь:** Команды на доработку или переход к следующему этапу

---

## 📁 Текущая структура проекта

```
Contract-AI-System-/
├── src/
│   ├── api/contracts/routes.py          # Существующие endpoints
│   ├── models/database.py               # Старые модели (Contract, AnalysisResult)
│   ├── services/
│   │   ├── document_parser.py           # DOCX/PDF парсинг
│   │   └── (НОВОЕ) idp_orchestrator.py  # Будущий оркестратор IDP
│   ├── agents/contract_analyzer_agent.py # Multi-agent система
│   └── schemas/
│       └── (НОВОЕ) idp_schemas.py       # Pydantic схемы
├── alembic/versions/                    # Миграции БД
├── docs/
│   ├── IDP_INTEGRATION_CONCEPT.md       # Старая концепция (устарела)
│   └── (НОВОЕ) ARCHITECTURE_V2.md       # Новая архитектура
└── current.md                           # ЭТОТ ФАЙЛ
```

---

## ⚠️ Важные напоминания и ОБЯЗАТЕЛЬНЫЕ требования

1. **КОД ПИШЕТСЯ ТОЛЬКО ПО КОМАНДЕ ПОЛЬЗОВАТЕЛЯ!**
2. Файл `current.md` обязателен к прочтению после каждой паузы
3. Этот файл НЕ растет, а ОБНОВЛЯЕТСЯ (прогресс, статусы чек-поинтов)
4. Перед началом Stage - согласование с пользователем
5. Тестирование после каждого значимого коммита

### 🔴 ОБЯЗАТЕЛЬНЫЕ требования к реализации:

6. **UI для тестирования:** Для КАЖДОГО Stage создавать интерфейс тестирования (ручной режим + визуальное отображение результатов)
7. **Человек в цикле:** Все действия системы (генерация протокола разногласий, извлечение данных) - только после одобрения пользователем
8. **Рекомендации системы:** Система выдает не только расхождения с эталонным договором, но и свои рекомендации для текущей ситуации
9. **Админ-консоль обязательна:** Максимум показателей в основном интерфейсе + дополнительно Streamlit dashboard
10. **RAG на каждом этапе:** RAG участвует как фильтр на всех этапах обработки
11. **Настройка режимов через UI:** Интерфейс для переключения между режимами работы (полная нагрузка / последовательный / ручной)

---

## 📊 Прогресс

**Текущий этап:** Stage 0 (✅ ЗАВЕРШЕН) → Переход к Stage 1
**Завершено:** 1/5 этапов (20%)
**Следующий шаг:** Stage 1 - Post-Execution MVP

### ✅ Stage 0: Инфраструктура (ЗАВЕРШЕН)
- ✅ 0.1: Создано 6 миграций для 14 таблиц (pgvector, negotiation, system_config, knowledge_base, llm_metrics)
- ✅ 0.2: Настроена конфигурация LLM (DeepSeek, Claude, GPT-4o, GPT-4o-mini)
- ✅ 0.3: Реализован Smart Router (RAG-assisted, fallback, cost estimation)
- ✅ 0.4: Создан RAG Service (pgvector, semantic search, knowledge base)
- ✅ 0.5: Создан System Config Service (3 режима работы, динамическая конфигурация)
- ✅ 0.6: Streamlit Admin Dashboard (5 страниц: Dashboard, Config, Metrics, RAG, Tests)
- ✅ 0.7: UI тестирования инфраструктуры

---

## 🔧 Технический стек

- **Backend:** FastAPI (Python 3.11+)
- **Database:** PostgreSQL 16+ (pgvector extension)
- **ORM:** SQLAlchemy 2.x
  - **Зачем:** Упрощает работу с БД через Python объекты, автоматические миграции (Alembic), type safety, защита от SQL injection
- **Validation:** Pydantic v2
- **OCR:** PaddleOCR + LayoutLMv3
- **NLP:** SpaCy (Level 1 extraction)
- **LLM:** DeepSeek-V3 (primary), Claude 4.5 Sonnet (expert), GPT-4o (reserve), GPT-4o-mini (testing)
- **Vector DB:** pgvector (PostgreSQL extension)
- **RAG:** ChromaDB + pgvector для контекстной фильтрации
- **Queue:** Redis + Celery (для асинхронной обработки)
- **UI:** React/Next.js (основной интерфейс)
- **Admin Dashboard:** Streamlit (быстрый dashboard для метрик и мониторинга)

---

## 📝 Intermediate JSON Format

Стандартный формат обмена между AI и БД:

```json
{
  "metadata": { "doc_type": "supply", "confidence": 0.95 },
  "financials": {
    "amount": 100000,
    "currency": "RUB",
    "vat_included": true
  },
  "payment_schedule": [
    { "type": "prepayment", "percent": 30, "due": "2026-02-01" }
  ],
  "rules": [
    {
      "type": "penalty",
      "trigger": "delay",
      "description": "0.1% per day limited to 10%",
      "structured_formula": { "rate": 0.001, "cap": 0.1 }
    }
  ]
}
```
