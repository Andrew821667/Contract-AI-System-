# 🔗 Contract AI System - Integration Guide

Руководство по интеграции системы с вашим Next.js сайтом (Vessel)

---

## 🎯 Два варианта запуска

### **Вариант 1: Быстрый демо с Ngrok (СЕГОДНЯ)**
Для показа клиентам и тестирования - **5 минут**

### **Вариант 2: Production с доменом (ПОТОМ)**
Для полноценного запуска - **30 минут**

---

## ⚡ Вариант 1: Быстрый демо с Ngrok

### Шаг 1: Запуск на VPS сервере

```bash
# На вашем VPS сервере
cd /opt/Contract-AI-System-

# Запустите демо
./start-demo-ngrok.sh
```

**Что произойдёт:**
1. ✅ Автоматически установит Ngrok (если нет)
2. ✅ Проверит здоровье системы
3. ✅ Создаст публичный URL
4. ✅ Выведет ссылку для клиентов

**Вывод будет таким:**
```
===================================================
🎉 Contract AI System is Live!
===================================================

Public URL: https://abc-123-def.ngrok-free.app

Share this URL with your clients to demo the system!

✅ Contract Analysis (AI-powered)
✅ Document Generation
✅ Risk Assessment
✅ Multi-language support
```

### Шаг 2: Настройка Ngrok (первый раз)

Если Ngrok не настроен:

1. **Зарегистрируйтесь:** https://dashboard.ngrok.com/signup
2. **Получите authtoken:** https://dashboard.ngrok.com/get-started/your-authtoken
3. **Настройте:**
```bash
ngrok config add-authtoken YOUR_AUTH_TOKEN
```
4. **Запустите снова:**
```bash
./start-demo-ngrok.sh
```

### Шаг 3: Добавьте ссылку на ваш Next.js сайт

Скопируйте URL из вывода и добавьте на сайт:

```tsx
// app/page.tsx или components/Hero.tsx
export default function HomePage() {
  return (
    <section className="hero">
      <h1>Юридические услуги с AI</h1>
      <p>Автоматизируем работу с договорами</p>

      {/* Добавьте эту кнопку */}
      <a
        href="https://abc-123-def.ngrok-free.app"  {/* Ваш Ngrok URL */}
        target="_blank"
        rel="noopener noreferrer"
        className="btn btn-primary"
      >
        Попробовать AI Анализ →
      </a>
    </section>
  );
}
```

**Плюсы Ngrok:**
- ✅ Работает сразу (5 минут)
- ✅ Бесплатно
- ✅ HTTPS из коробки
- ✅ Не нужен домен

**Минусы Ngrok:**
- ⚠️ URL меняется при перезапуске
- ⚠️ Есть лимиты на бесплатном плане
- ⚠️ Меньше доверия от клиентов

---

## 🌐 Вариант 2: Production с доменом

### Рекомендуемые домены для вашего проекта:

| Домен | Цена/год | Где купить | Рекомендация |
|-------|----------|------------|--------------|
| **dogovor-ai.ru** | 300₽ | reg.ru | ⭐⭐⭐⭐⭐ Понятно сразу |
| **legal-ai.ru** | 300₽ | reg.ru | ⭐⭐⭐⭐⭐ Профессионально |
| **yurdelo.ru** | 300₽ | reg.ru | ⭐⭐⭐⭐ Креативно |
| **legai.ru** | 300₽ | reg.ru | ⭐⭐⭐⭐ Короткий |

### Шаг 1: Покупка домена (5 минут)

#### На REG.RU:
1. Зайдите на https://www.reg.ru
2. Проверьте доступность домена: `dogovor-ai.ru`
3. Добавьте в корзину
4. Зарегистрируйтесь и оплатите (~300₽)

#### На Namecheap (если хотите .com/.ai):
1. Зайдите на https://www.namecheap.com
2. Найдите домен
3. Оплатите ($10-80/год)

### Шаг 2: Настройка DNS (сразу после покупки)

В панели управления доменом (reg.ru):

```
Мои домены → dogovor-ai.ru → DNS-серверы и зона

Добавить A-запись:
┌─────────────────────────────┐
│ Тип:  A                     │
│ Имя:  @ (или оставить пусто)│
│ IP:   123.45.67.89          │ ← IP вашего VPS
│ TTL:  3600                  │
└─────────────────────────────┘

Нажать "Добавить"
```

**Проверка DNS (через 5-30 минут):**
```bash
# На вашем компьютере
nslookup dogovor-ai.ru
# Должен вернуть IP вашего сервера
```

### Шаг 3: Настройка на VPS сервере

```bash
# 1. Подключитесь к серверу
ssh root@your-server-ip

# 2. Перейдите в директорию проекта
cd /opt/Contract-AI-System-

# 3. Обновите конфигурацию
nano .env.production

# Измените:
DOMAIN=dogovor-ai.ru
CORS_ORIGINS=https://your-main-site.com,https://dogovor-ai.ru

# 4. Получите бесплатный SSL сертификат
sudo apt install certbot -y
sudo certbot certonly --standalone -d dogovor-ai.ru

# 5. Скопируйте сертификаты
mkdir -p nginx/ssl
sudo cp /etc/letsencrypt/live/dogovor-ai.ru/fullchain.pem nginx/ssl/
sudo cp /etc/letsencrypt/live/dogovor-ai.ru/privkey.pem nginx/ssl/

# 6. Обновите Nginx конфигурацию
nano nginx/conf.d/contract-ai.conf

# Раскомментируйте блок:
# server {
#     listen 443 ssl http2;
#     server_name dogovor-ai.ru;
#     ...
# }

# 7. Перезапустите систему
./deploy.sh
# Выберите: 5) Restart
```

### Шаг 4: Проверка

```bash
# Проверьте HTTP
curl http://dogovor-ai.ru/health

# Проверьте HTTPS
curl https://dogovor-ai.ru/health

# Откройте в браузере
# https://dogovor-ai.ru
```

### Шаг 5: Автообновление SSL (раз в 3 месяца)

```bash
# Настройка автообновления через Cron
sudo crontab -e

# Добавьте:
0 0 1 * * certbot renew --quiet && docker-compose -f /opt/Contract-AI-System-/docker-compose.production.yml restart nginx
```

---

## 🔗 Интеграция с Next.js сайтом (Vessel)

### Вариант A: Простая кнопка

```tsx
// app/page.tsx
export default function HomePage() {
  return (
    <div>
      <h1>Юридические услуги</h1>

      <a
        href="https://dogovor-ai.ru"
        target="_blank"
        rel="noopener noreferrer"
        className="inline-flex items-center px-6 py-3 bg-blue-600 hover:bg-blue-700 text-white font-semibold rounded-lg"
      >
        Анализ договоров AI →
      </a>
    </div>
  );
}
```

### Вариант B: Готовый компонент

Скопируйте файл `integration/nextjs-component.tsx` в ваш проект:

```bash
# На вашем компьютере с Next.js проектом
# Скопируйте файл из репозитория
cp integration/nextjs-component.tsx components/ContractAIDemo.tsx
```

Используйте:

```tsx
// app/page.tsx
import { ContractAIDemo } from '@/components/ContractAIDemo';

export default function HomePage() {
  return (
    <div>
      {/* Вариант 1: Простая кнопка */}
      <ContractAIDemo
        demoUrl="https://dogovor-ai.ru"
        variant="button"
      />

      {/* Вариант 2: Карточка с описанием */}
      <ContractAIDemo
        demoUrl="https://dogovor-ai.ru"
        variant="card"
        theme="light"
      />

      {/* Вариант 3: Полноширинный баннер */}
      <ContractAIDemo
        demoUrl="https://dogovor-ai.ru"
        variant="banner"
        theme="dark"
      />
    </div>
  );
}
```

### Вариант C: Встроенный iframe

Если хотите встроить систему прямо на страницу:

```tsx
// app/ai-analysis/page.tsx
export default function AIAnalysisPage() {
  return (
    <div className="w-full h-screen">
      <iframe
        src="https://dogovor-ai.ru"
        className="w-full h-full border-0"
        allow="clipboard-write"
      />
    </div>
  );
}
```

---

## 📊 Сравнение вариантов

| Параметр | Ngrok (Демо) | Домен (Production) |
|----------|--------------|-------------------|
| **Время запуска** | 5 минут | 30 минут |
| **Стоимость** | Бесплатно | 300₽-$80/год |
| **URL** | Случайный | Ваш бренд |
| **HTTPS** | ✅ Да | ✅ Да |
| **Постоянство** | ❌ Меняется | ✅ Постоянный |
| **Доверие клиентов** | ⚠️ Среднее | ✅ Высокое |
| **Для демо** | ✅ Отлично | ✅ Отлично |
| **Для продакшена** | ❌ Нет | ✅ Да |

---

## 🎯 Рекомендуемый план

### Неделя 1 (Сегодня): Демо с Ngrok
```bash
# 1. Запустите демо
./start-demo-ngrok.sh

# 2. Получите URL
# https://abc-123.ngrok-free.app

# 3. Добавьте кнопку на сайт
# 4. Покажите клиентам
```

**Результат:** Клиенты могут протестировать систему прямо сейчас

### Неделя 2: Купите домен и настройте production
```bash
# 1. Купите домен (dogovor-ai.ru)
# 2. Настройте DNS
# 3. Установите SSL
# 4. Обновите ссылку на сайте
```

**Результат:** Профессиональный постоянный URL

---

## 🛠️ Troubleshooting

### Ngrok не запускается
```bash
# Проверьте authtoken
ngrok config check

# Переустановите
rm -f /usr/local/bin/ngrok
./start-demo-ngrok.sh
```

### SSL сертификат не устанавливается
```bash
# Проверьте, что порт 80 свободен
sudo netstat -tlnp | grep :80

# Остановите Nginx временно
docker-compose -f docker-compose.production.yml stop nginx

# Получите сертификат
sudo certbot certonly --standalone -d dogovor-ai.ru

# Запустите Nginx обратно
docker-compose -f docker-compose.production.yml start nginx
```

### DNS не обновляется
```bash
# Проверьте DNS
nslookup dogovor-ai.ru

# Очистите кэш DNS (на вашем компьютере)
# Windows: ipconfig /flushdns
# Mac: sudo dscacheutil -flushcache
# Linux: sudo systemd-resolve --flush-caches

# Подождите до 30 минут для глобального распространения
```

---

## 📞 Поддержка

### Проверка статуса системы
```bash
# Статус контейнеров
./deploy.sh
# Выбрать: 7) Show status

# Логи
./deploy.sh
# Выбрать: 6) Show logs

# Health check
curl http://localhost/health
```

### Полезные команды
```bash
# Перезапуск
./deploy.sh → 5) Restart

# Остановка
./deploy.sh → 4) Stop

# Обновление
./deploy.sh → 2) Update
```

---

## ✅ Checklist

### Для демо (Ngrok):
- [ ] Ngrok установлен и настроен
- [ ] Система запущена (./deploy.sh → 3)
- [ ] start-demo-ngrok.sh выполнен
- [ ] URL получен и работает
- [ ] Кнопка добавлена на Next.js сайт
- [ ] Клиенты могут тестировать

### Для production (Домен):
- [ ] Домен куплен
- [ ] DNS A-запись настроена
- [ ] SSL сертификат получен
- [ ] Nginx настроен на HTTPS
- [ ] .env.production обновлён
- [ ] Система перезапущена
- [ ] https://ваш-домен.ru работает
- [ ] Автообновление SSL настроено
- [ ] Ссылка обновлена на сайте

---

## 🎉 Готово!

Теперь у вас есть работающая интеграция Contract AI System с вашим Next.js сайтом!

**Следующие шаги:**
1. Покажите демо первым клиентам
2. Соберите обратную связь
3. При необходимости купите домен для постоянного URL
4. Масштабируйте!

**Удачи! 🚀**
